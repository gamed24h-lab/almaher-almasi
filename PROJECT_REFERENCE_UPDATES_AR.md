# تحديثات المرجع — النسخة الشاملة

> هذا سجل إضافي مرتبط بالمرجع الرئيسي `PROJECT_REFERENCE_AR.md` لحفظ حالة التنفيذ الفعلية وعدم فقد أي بند.

## 2026-08-22 — مراجعة الواجهة الفعلية
- المالية: إضافة مركز تقارير وطباعة وصلاحيات `printReports` و`reports` مع تصدير CSV.
- التعريب: توسيع ترجمة الحالات الداخلية الشائعة مثل `available / open / closed / posted / in_progress` وغيرها.
- الجداول العامة: إضافة قاموس مركزي لعناوين الأعمدة الشائعة حتى لا تظهر عناوين تقنية خام مثل `title / priority / status / name / phone`.
- الفروع: إضافة رفع شعار الفرع مباشرة من الجهاز مع ضغط الصورة ومعاينتها وإمكانية الرجوع للشعار الرسمي.
- هوية المطور: توحيد مصدر الاسم والمسمى والهاتف في الهيدر والفوتر والتذكرة وصفحة الدخول.

## 2026-08-22 — دفعة النسخة الشاملة / كتلة التنفيذ
- **أقرب الرحلات:** عرض الرحلات الحالية والقادمة فقط، مع استبعاد الملغاة/المكتملة/المغلقة والرحلات ذات التاريخ الماضي، وترتيبها زمنيًا.
- **تنسيق الرحلة:** إظهار يوم الرحلة + التاريخ + الوقت + الاتجاه + كود الرحلة بصورة مفهومة.
- **هوية المطور:** حساب developer الحقيقي يستخدم ملف المطور المركزي بدل وجود أكثر من هوية ظاهرة.
- **المسميات:** ربط اسم النظام ووصفه وعنوان/وصف الصفحة الرئيسية وتذييل التذكرة والتقارير بمركز إعدادات المطور، مع سجل نسخ سابقة وإمكانية Rollback.
- **الثيمات:** توفير ثيمات الماهر / المحيط / الزمرد / الجرافيت مع حفظ اختيار الواجهة للمستخدم على الجهاز.
- **اللغات:** العربية والإنجليزية والتركية والهندية والإيطالية والفرنسية والأردية مع RTL/LTR، ومبدّل لغة في واجهة الموظف والدخول وبوابة العميل.
- **ترجمة الواجهة القديمة:** إضافة طبقة `RuntimeTranslationSafe` لتغطية النصوص العربية الثابتة التي ما زالت داخل الموديولات القديمة، وتطبيق الترجمة على النصوص المعروفة فقط دون تغيير بيانات العملاء أو الموظفين.
- **الطباعة المعزولة:** إضافة طبقة عامة `PrintIsolation` ومحرك `printElement` بحيث أي طباعة قديمة تعتمد على `window.print()` يتم توجيهها إلى المستند/الكشف المحدد بدل طباعة الـSidebar والهيدر وبقية واجهة النظام.
- **Safari/iPhone:** الطباعة المستقلة تستخدم iframe مخفي بدل Popup؛ لا تعتمد على `window.open` في محرك الطباعة العام.
- **المالية:** مركز تقارير يتيح اختيار نوع التقرير: ملخص مالي / مصروفات / حركات / خزن / ورديات / مستحقات الموردين، مع الفرع والفترة ثم طباعة أو CSV.
- **التشغيل:** زر كشف الركاب صار يطبع `.operations-sheet` فقط في A4 أفقي من خلال `printElement`.
- **السكن:** عمليات الطباعة القديمة التي تضع `housingPrint` يتم اعتراضها عالميًا واختيار `.housing-manifest` فقط.
- **التذكرة:** أوضاع A4 / 80mm / 58mm يتم اعتراضها مع اختيار `.ticket-page` فقط والاحتفاظ بلغة التذكرة ووضع الطباعة.
- **بوابة العميل:** الطباعة العامة تختار بطاقة التذكرة الخاصة بالعميل فقط عند وجودها.
- **الاسترداد:** سند الاسترداد أصبح مستندًا مستقلًا داخل iframe بدون Popup، مع عناوين متعددة اللغات حسب لغة النظام.
- **التقارير والتصدير:** سجل التقارير يستخدم مستندًا مستقلاً عبر محرك الطباعة بدل طباعة صفحة النظام.
- **الحماية:** فصل إدارة بيانات الموظفين عن إدارة الصلاحيات، ومنع تعديل الموظف الأعلى إداريًا أو منح صلاحية لا يملكها المسؤول، وحجز صلاحيات المطور للحساب الحقيقي مع حماية Server-Side.
- **السكن المشترك:** رجال فقط، غرف خماسية، وتطبيق أولوية تجميع الجنسيات العربية معًا قدر الإمكان.
- **لغة التذكرة:** اقتراح اللغة حسب جنسية العميل مع إمكانية التغيير اليدوي قبل الطباعة.
- **واجهة الدخول Desktop:** تثبيت Layout منفصل للشاشات الكبيرة؛ الهوية والكارت أصبحا يستغلان عرض الشاشة بدل ظهور نموذج موبايل صغير وسط سطح المكتب، مع بقاء Layout الجوال منفصلًا.
- **صلاحيات الفروع:** إضافة صلاحيات مستقلة `viewBranches` لعرض الفروع، `addBranches` لإضافة فرع، `editBranches` لتعديل الفروع، و`manageCompanyProfile` لتعديل بيانات الشركة العامة، مع الإبقاء على `manageBranches` كصلاحية شاملة للتوافق.
- **واجهة الفروع:** زر إضافة الفرع لا يظهر إلا لمن يملك صلاحية الإضافة، وزر التعديل لا يظهر إلا لمن يملك صلاحية التعديل، وبيانات الشركة العامة لا تظهر إلا لصاحب صلاحيتها.
- **حماية الفروع Server-Side:** مسار `save_branch` يتحقق من إضافة/تعديل على الخادم، و`company_settings_save` يتحقق من صلاحية تعديل بيانات الشركة؛ إخفاء الزر وحده لا يكفي.
- **قوالب الموظفين:** صلاحيات الفروع أصبحت ضمن قائمة الصلاحيات التشغيلية القابلة للتفويض وفق الهرم الإداري؛ قالب مدير الفرع يشمل عرض/تعديل الفروع دون منحه إنشاء فروع جديدة أو تعديل بيانات الشركة العامة تلقائيًا.

## سياسة الانتقال من التدريب إلى التشغيل
- النظام يظل في وضع التدريب حتى اجتياز بوابة جاهزية التشغيل.
- الانتقال إلى Production لا يكون تغيير تسمية فقط؛ يسبقه Snapshot/Backup، فحص صحة النظام والبيانات، فحص الأسرار والصلاحيات، ومراجعة البيانات المصنفة تدريبية.
- لا يجوز حذف إعدادات النظام والفروع والموظفين والصلاحيات والقوالب والشعارات أثناء تنظيف بيانات التدريب.
- أي تنظيف قبل التشغيل يجب أن يستهدف البيانات التدريبية فقط مع سجل تدقيق وإمكانية استعادة.
- بعد التحويل يتم تنفيذ Post-Activation checks للحفظ والقراءة والصلاحيات والطباعة والمالية والاسترداد من جلسة/جهاز آخر قبل اعتماد التشغيل النهائي.

## قاعدة الاعتماد
- المرجع التنفيذي هو الكود الموجود على `main` بعد نجاح الـBuild والاختبارات العملية.
- لا يتم إعلان Production Ready لمجرد نجاح الـBuild؛ الاعتماد يتطلب اختبارات Desktop أولًا على Chrome/Edge ثم حد توافق آمن على iPhone/Safari.
