# مرجع بوابة الانتقال الآمن إلى Production — 2026-08-23

هذا الملحق جزء من المرجع التنفيذي لنظام الماهر الماسي، ويثبت قواعد الانتقال من وضع التدريب إلى التشغيل الفعلي بدون حذف أو خلط بيانات بالخطأ.

## ما تم تنفيذه
- إضافة Worker طبقي جديد `worker/production-index.js` فوق طبقة التدقيق والأمان الحالية، بدون إزالة `audit-index.js` أو `secure-index.js`.
- تحديث `wrangler.toml` ليبدأ من `worker/production-index.js` مع استمرار `keep_vars = true` لحماية Variables/Secrets في Cloudflare.
- إضافة API محمية للمطور الحقيقي فقط: `/api/production/readiness`.
- الفحص يراجع صحة API، وجود الفروع، وجود موظفين نشطين، وجود حقول `data_environment` على الجداول التشغيلية الحرجة، وعدم وجود موافقات حساسة معلقة.
- الفحص يعرض أعداد بيانات `training` و`production` لكل جدول حرج بدل الاعتماد على التخمين.
- إضافة API محمية: `/api/production/snapshot` لإنشاء Snapshot تشغيلية من الخادم قبل أي Go-Live.
- إبقاء `/api/production/activate` مقفولًا عمدًا في هذه المرحلة وإرجاع Locked، حتى لا يمكن تفعيل Production بالخطأ قبل اكتمال شروط الأمان.
- إضافة واجهة `ProductionControl.jsx` داخل صفحة المطور تعرض فحوصات الجاهزية، أعداد الموظفين، الموافقات المعلقة، وعزل بيانات التدريب/التشغيل.
- لا يوجد في هذه الدفعة أي حذف لبيانات التدريب ولا تغيير لحالة البيانات الحالية.

## سياسة الاعتماد قبل فتح التفعيل
لا يفتح التفعيل الفعلي إلا بعد تحقق الشروط التالية كلها:
1. نجاح فحص الجاهزية بدون Blockers.
2. إنشاء Snapshot معتمدة قبل التنفيذ.
3. معاينة صريحة لما سيتم حذفه من بيانات Training فقط.
4. تأكيد مزدوج من المطور للعملية الحساسة.
5. وجود Rollback صالح ومختبر.
6. تنفيذ Post-Activation Check بعد التحويل للتأكد من تسجيل الدخول والحفظ والقراءة والصلاحيات والطباعة والمالية والاسترداد.

## قاعدة عدم فقد البيانات
- الفروع والموظفون والصلاحيات والشعارات والقوالب وإعدادات المطور واللغات لا تعتبر بيانات تدريب قابلة للحذف تلقائيًا.
- أي تنظيف مستقبلي يجب أن يعتمد على `data_environment='training'` فقط، وليس على التاريخ أو اسم السجل أو التخمين.
- لا يسمح لمسار المتصفح بتنفيذ Purge مباشر بدون حماية Server-Side.
