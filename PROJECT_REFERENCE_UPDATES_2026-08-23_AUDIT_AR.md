# ملحق المرجع — سجل التدقيق والأمان — 2026-08-23

هذا الملحق مرتبط بالمرجع الرئيسي `PROJECT_REFERENCE_AR.md` وملحق التنفيذ `PROJECT_REFERENCE_UPDATES_2026-08-23_AR.md`.

## سجل التدقيق والأمان
- إضافة Worker Wrapper جديد `worker/audit-index.js` فوق `worker/secure-index.js` بدون إزالة طبقة الحماية الحالية.
- تعديل `wrangler.toml` ليبدأ من `worker/audit-index.js` مع الإبقاء على `keep_vars = true` كما هو.
- إضافة مسار قراءة آمن `GET /api/audit` لسجل `activity_events`.
- السماح بقراءة سجل التدقيق فقط للمطور، المدير العام، أو من لديه صلاحية مناسبة مثل `managePermissions`/`auditLog`.
- تطبيق نطاق الفروع على سجل التدقيق؛ الحسابات غير العامة لا ترى إلا أحداث فرعها.
- إضافة بحث وتصنيف للأحداث: أمان وصلاحيات / مالية / مطور ونظام.
- إضافة تسجيل تلقائي للعمليات الكتابية الناجحة التي تمر عبر `/api/admin` و`/api/module` و`/api/mega` و`/api/platform` مع بيانات وصفية آمنة فقط، بدون تخزين كلمات مرور أو أسرار أو محتوى الطلب الكامل.
- إضافة `AuditPanel.jsx` داخل صفحة المطور لعرض السجل، المؤشرات، البحث، التصنيف والطباعة المستقلة.
- الطباعة من مركز التدقيق تطبع السجل فقط ولا تطبع واجهة النظام كاملة.

## قاعدة الاعتماد
- مركز التدقيق Read-only للمراجعة ولا يوفر تعديل أو حذف سجلات `activity_events` من الواجهة.
- أي توسيع لاحق للحذف/الأرشفة يجب أن يظل صلاحية مطور منفصلة ولا يتم دمجه في زر عرض السجل.
