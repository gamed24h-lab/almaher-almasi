import React,{useEffect,useMemo,useState} from 'react';
import QRCode from 'qrcode';
import {ArrowRight,Printer,MessageCircle,MapPin,BusFront,Hotel,Armchair,Phone,Languages,FileDown} from 'lucide-react';
import {useAppData} from '../../core/AppDataContext.jsx';
import {suggestedLanguageForNationality} from '../../core/LanguageContext.jsx';
import {useSystemBrand} from '../../core/SystemBrandContext.jsx';
import {api} from '../../lib/api.js';
import {PageHeader,Button,Card,Loading,ErrorBox,Badge} from '../../components/UI.jsx';
import {money,phoneWa,journeyLabel} from '../../lib/format.js';
import {branchLogo} from '../../lib/branding.js';
import {PRINT_LANGUAGES,printDirection,pt} from '../../lib/printI18n.js';
import {printElement,saveElementAsPdf} from '../../lib/print.js';

const str=v=>String(v??'');const lower=v=>str(v).toLowerCase();const DAY_NAMES=['الأحد','الاثنين','الثلاثاء','الأربعاء','الخميس','الجمعة','السبت'];const weekdayAr=d=>d?DAY_NAMES[new Date(d+'T12:00:00').getDay()]||'':'';function first(...vals){return vals.find(v=>v!==undefined&&v!==null&&str(v).trim()!=='')??''}
export default function TicketPage({bookingNo,go}){
 const {data}=useAppData();const {profile:developer,labels,config}=useSystemBrand();const b=data.bookings.find(x=>str(x.booking_number)===str(bookingNo));const passengers=data.passengers.filter(x=>str(x.booking_id)===str(b?.id));const [qr,setQr]=useState(''),[locationQr,setLocationQr]=useState(''),[ops,setOps]=useState(null),[terms,setTerms]=useState([]),[error,setError]=useState(''),[printLanguage,setPrintLanguage]=useState('ar'),[languageTouched,setLanguageTouched]=useState(false);const trip=data.trips.find(x=>str(x.id)===str(b?.trip_id));const returnTrip=data.trips.find(x=>str(x.id)===str(b?.return_trip_id));const branch=data.branches.find(x=>str(x.id)===str(b?.branch_id));const branchContact=data.branchContacts.find(x=>str(x.branch_id)===str(b?.branch_id));const tripBranch=(data.tripBranches||[]).find(x=>str(x.trip_id)===str(b?.trip_id)&&str(x.branch_id)===str(b?.branch_id));const locationText=first(tripBranch?.boarding_point,branch?.address,branchContact?.address);const locationUrl=first(branch?.map_url,branch?.mapUrl,branchContact?.map_url,branchContact?.mapUrl,locationText?`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(locationText)}`:'');const customerNationality=first(b?.customer_nationality,passengers[0]?.nationality,b?.snapshot?.nationality);
 useEffect(()=>{if(!b||languageTouched)return;setPrintLanguage(suggestedLanguageForNationality(customerNationality))},[b?.id,customerNationality,languageTouched]);
 useEffect(()=>{if(!b)return;QRCode.toDataURL(`ALMAHER|BOOKING=${b.booking_number}`,{width:280,margin:1,errorCorrectionLevel:'M'}).then(setQr).catch(()=>{});if(locationUrl)QRCode.toDataURL(locationUrl,{width:220,margin:1,errorCorrectionLevel:'M'}).then(setLocationQr).catch(()=>{});api.module('tickets').then(setOps).catch(e=>setError(e.message));api.mega('ticket_terms',{},'GET').then(x=>setTerms(Array.isArray(x?.terms)?x.terms:[])).catch(()=>{})},[b?.id,locationUrl]);
 const seatByPassenger=useMemo(()=>{const map=new Map();for(const x of ops?.seat_assignments||[]){if(!['assigned','hold'].includes(lower(x.status||'assigned')))continue;if(str(x.booking_id)===str(b?.id)||passengers.some(p=>str(p.id)===str(x.passenger_id))){const k=str(x.passenger_id||'booking');const arr=map.get(k)||[];arr.push(x);map.set(k,arr)}}return map},[ops,b?.id,passengers]);
 const roomByPassenger=useMemo(()=>{const roomMap=new Map((ops?.hotel_rooms||[]).map(x=>[str(x.id),x]));const tripHotelMap=new Map((ops?.trip_hotels||[]).map(x=>[str(x.id),x]));const hotelMap=new Map((ops?.hotels||[]).map(x=>[str(x.id),x]));const out=new Map();for(const a of ops?.room_assignments||[]){if(lower(a.status||'assigned')==='released')continue;const room=roomMap.get(str(a.hotel_room_id));if(!room)continue;const th=tripHotelMap.get(str(room.trip_hotel_id));const hotel=hotelMap.get(str(th?.hotel_id));out.set(str(a.passenger_id),{assignment:a,room,tripHotel:th,hotel})}return out},[ops]);
 if(!b)return <Card>الحجز غير موجود في البيانات الحالية.</Card>;const paid=Number(b.paid_amount||0),total=Number(b.total_price||0),remaining=Math.max(0,total-paid);const L=(key,f='')=>pt(printLanguage,key,f);const dir=printDirection(printLanguage);const printOptions=mode=>({title:`تذكرة ${b.booking_number} — ${mode==='a4'?'A4':mode+'mm'}`,pageSize:mode==='a4'?'A4':`${mode}mm`,orientation:'portrait',lang:printLanguage,dir,singlePage:mode==='a4',bodyAttributes:{'data-print-mode':mode,'data-print-language':printLanguage}});const ticketNode=()=>document.querySelector('.ticket-page');const doPrint=mode=>{setError('');const el=ticketNode();if(!el)return setError('تعذر تجهيز التذكرة للطباعة.');try{printElement(el,printOptions(mode))}catch(e){setError(e.message||'تعذر فتح الطباعة.')}};const doPdf=mode=>{setError('');const el=ticketNode();if(!el)return setError('تعذر تجهيز التذكرة للحفظ PDF.');try{saveElementAsPdf(el,{...printOptions(mode),title:`${b.booking_number}-${mode==='a4'?'A4':mode+'mm'}-PDF`})}catch(e){setError(e.message||'تعذر تجهيز ملف PDF.')}};const wa=()=>{const route=trip?`${trip.from_city||trip.origin||'—'} ← ${trip.to_city||trip.destination||'—'}`:'';const msg=[`شركة الماهر الماسي`,`تذكرة الحجز: ${b.booking_number}`,`العميل: ${b.customer_name||''}`,route?`الرحلة: ${route}`:'',trip?.departure_date?`التاريخ: ${weekdayAr(trip.departure_date)} ${trip.departure_date} ${trip.departure_time||''}`:'',`المدفوع: ${money(paid)}`,remaining?`المتبقي: ${money(remaining)}`:'الحجز مسدد بالكامل'].filter(Boolean).join('\n');window.open(`https://wa.me/${phoneWa(b.customer_phone)}?text=${encodeURIComponent(msg)}`,'_blank')};const phone=first(branchContact?.phone,branchContact?.whatsapp,branch?.whatsapp,branch?.phone);const dense=passengers.length>12?'ultra-dense':passengers.length>6?'dense':'';const snap=b.snapshot&&typeof b.snapshot==='object'?b.snapshot:{};const privateType=snap.privateRoomType||(Array.isArray(b.private_room_types)?b.private_room_types[0]:'');const privateTypeLabel={single:'مفردة',double:'مزدوجة',triple:'ثلاثية',quad:'رباعية',quint:'خماسية'}[privateType]||privateType;const ticketCancelled=['cancelled','refunded'].includes(lower(b.status))||lower(trip?.status)==='cancelled';const showOutbound=lower(b.journey_mode)!=='returnonly';const showReturn=['roundtrip','separate','returnonly'].includes(lower(b.journey_mode));const returnInfo=returnTrip||trip;const returnDate=returnTrip?.departure_date||trip?.return_date||trip?.departure_date;const returnTime=returnTrip?.departure_time||trip?.return_time||'';const defaultTerms=[L('defaultTerm1'),L('defaultTerm2'),L('defaultTerm3')];
 return <><PageHeader title={L('ticket')} subtitle={`${L('bookingNo')} ${b.booking_number}`} actions={<><Button onClick={()=>go('/bookings/'+b.booking_number)}><ArrowRight size={16}/> {L('back')}</Button><label className="btn" style={{gap:7}}><Languages size={16}/><select value={printLanguage} onChange={e=>{setLanguageTouched(true);setPrintLanguage(e.target.value)}} style={{border:0,padding:0,width:'auto',background:'transparent'}}>{PRINT_LANGUAGES.map(([code,label])=><option key={code} value={code}>{label}</option>)}</select></label><Button onClick={()=>doPrint('a4')}><Printer size={16}/> A4</Button><Button onClick={()=>doPrint('80')}><Printer size={16}/> 80mm</Button><Button onClick={()=>doPrint('58')}><Printer size={16}/> 58mm</Button><Button onClick={()=>doPdf('a4')}><FileDown size={16}/> PDF A4</Button><Button onClick={()=>doPdf('80')}><FileDown size={16}/> PDF 80</Button><Button onClick={()=>doPdf('58')}><FileDown size={16}/> PDF 58</Button><Button variant="primary" onClick={wa}><MessageCircle size={16}/> واتساب</Button></>}/><ErrorBox error={error}/>{!ops&&!error&&<Loading text="استكمال بيانات المقعد والسكن..."/>}<article className={`ticket-page ${dense} ${ticketCancelled?'ticket-cancelled':''}`} dir={dir} lang={printLanguage}><header className="ticket-head"><div className="ticket-brand-lockup"><img className="ticket-logo" src={branchLogo(branch)} alt={`شعار ${branch?.name||'الماهر الماسي'}`}/><div><h1>{branch?.name||labels.system_name||'الماهر الماسي'}</h1><span>نُيسر دربك... لنطمئن قلبك</span></div></div><div className="ticket-number"><span>{L('bookingNo')}</span><b>{b.booking_number}</b><Badge tone={b.status==='cancelled'?'red':'green'}>{b.status||'confirmed'}</Badge></div></header><section className="ticket-hero-grid"><div><span>{L('customer')}</span><strong>{b.customer_name}</strong><small>{b.customer_phone}</small></div><div><span>{L('journeyType')}</span><strong>{journeyLabel(b.journey_mode)}</strong><small>{branch?.name||''}</small></div><div><span>{L('housing')}</span><strong>{b.accommodation_label||b.accommodation_type||L('noHousing')}</strong><small>{b.accommodation_type==='private'?[privateTypeLabel&&`غرفة ${privateTypeLabel}`,`${Number(b.private_rooms||snap.privateRooms||1)} غرفة`,snap.housingDays&&`${snap.housingDays} ${L('day')}`].filter(Boolean).join(' · '):''}</small></div><div><span>{L('paymentStatus')}</span><strong>{remaining>0?`${L('remaining')} ${money(remaining)}`:L('paidFull')}</strong><small>{b.payment_method||''}</small></div></section><section className="ticket-journeys">{showOutbound&&<JourneyBlock title={L('outbound')} trip={trip} date={trip?.departure_date} time={trip?.departure_time} boardingPoint={tripBranch?.boarding_point||b.snapshot?.boardingPoint} boardingTime={tripBranch?.boarding_time||b.snapshot?.boardingTime} unavailable={L('tripUnavailable')}/>} {showReturn&&<JourneyBlock title={L('return')} trip={returnInfo} date={returnDate} time={returnTime} reverse={!returnTrip&&lower(b.journey_mode)==='roundtrip'} unavailable={L('tripUnavailable')}/>}</section><section className="ticket-passenger-section"><div className="ticket-section-title"><h3>{L('passengers')}</h3><span>{passengers.length} {L('passenger')}</span></div><div className="ticket-passengers">{passengers.map((p,i)=>{const seats=seatByPassenger.get(str(p.id))||[];const room=roomByPassenger.get(str(p.id));return <div className="ticket-passenger" key={p.id||i}><div className="ticket-pax-main"><b>{i+1}. {p.full_name}</b><span>{p.identity_number} · {p.nationality||''}</span></div><div className="ticket-pax-tags">{seats.length?seats.map((s,j)=><span key={s.id||j}><Armchair size={12}/> {s.seat_no||s.seat_number||'—'}{s.segment_type?` · ${s.segment_type}`:''}</span>):<span><Armchair size={12}/> {L('seatUnset')}</span>}{room&&<span><Hotel size={12}/> {room.hotel?.name||L('hotel')} · {L('room')} {room.room?.room_no||'—'}</span>}</div></div>})}</div></section><section className="ticket-bottom"><div className="ticket-finance"><div><span>{L('total')}</span><b>{money(total)}</b></div><div><span>{L('paid')}</span><b>{money(paid)}</b></div><div><span>{L('remaining')}</span><b>{money(remaining)}</b></div></div><div className="ticket-qr-group">{qr&&<div><img src={qr} alt="QR"/><span>{L('bookingQr')}</span></div>}{locationQr&&<div><img src={locationQr} alt="QR location"/><span>{L('boardingLocation')}</span></div>}</div></section><section className="ticket-terms"><h3>{L('terms')}</h3><ol>{(terms.length?terms:defaultTerms).map((x,i)=><li key={i}>{typeof x==='string'?x:x?.[printLanguage]||x?.text||x?.ar}</li>)}</ol></section><footer className="ticket-contact"><div>{locationText&&<span><MapPin size={13}/> {locationText}</span>}{phone&&<span><Phone size={13}/> {phone}</span>}</div><small>{labels.ticket_footer||L('termsSnapshot')}</small>{config.show_profile_tickets!==false&&<small>{L('developer')}: {developer.display_name}{developer.phone?` · ${developer.phone}`:''}</small>}</footer></article></>;
}
function JourneyBlock({title,trip,date,time,boardingPoint,boardingTime,reverse=false,unavailable='بيانات الرحلة غير متاحة'}){if(!trip)return <div className="journey-block"><strong>{title}</strong><span>{unavailable}</span></div>;const from=reverse?(trip.to_city||trip.destination):(trip.from_city||trip.origin),to=reverse?(trip.from_city||trip.origin):(trip.to_city||trip.destination);return <div className="journey-block"><div className="journey-title"><BusFront size={17}/><strong>{title}</strong><span>{trip.trip_code||''}</span></div><div className="journey-route"><b>{from||'—'}</b><span>←</span><b>{to||'—'}</b></div><div className="journey-meta"><span>{weekdayAr(date)} · {date||'—'} {time||''}</span>{boardingPoint&&<span>{boardingPoint}{boardingTime?` · ${boardingTime}`:''}</span>}</div></div>}
