import React,{useEffect,useRef,useState} from 'react';import QRCode from 'qrcode';import {ArrowRight,Printer,MessageCircle} from 'lucide-react';import {useAppData} from '../../core/AppDataContext.jsx';import {PageHeader,Button,Card} from '../../components/UI.jsx';import {money,phoneWa} from '../../lib/format.js';
export default function TicketPage({bookingNo,go}){const {data}=useAppData();const b=data.bookings.find(x=>String(x.booking_number)===String(bookingNo)),p=data.passengers.filter(x=>String(x.booking_id)===String(b?.id));const [qr,setQr]=useState('');const ticket=useRef(null);useEffect(()=>{if(b)QRCode.toDataURL(`ALMAHER|BOOKING=${b.booking_number}`,{width:260,margin:1}).then(setQr)},[b]);if(!b)return <Card>الحجز غير موجود في البيانات الحالية.</Card>;const doPrint=(mode)=>{document.body.dataset.printMode=mode;window.print();setTimeout(()=>delete document.body.dataset.printMode,300)};const wa=()=>open(`https://wa.me/${phoneWa(b.customer_phone)}?text=${encodeURIComponent(`تذكرة الماهر الماسي — رقم الحجز ${b.booking_number}`)}`,'_blank');return <><PageHeader title="التذكرة" subtitle={`الحجز ${b.booking_number}`} actions={<><Button onClick={()=>go('/bookings/'+b.booking_number)}><ArrowRight size={16}/> رجوع</Button><Button onClick={()=>doPrint('a4')}><Printer size={16}/> A4</Button><Button onClick={()=>doPrint('80')}><Printer size={16}/> 80mm</Button><Button onClick={()=>doPrint('58')}><Printer size={16}/> 58mm</Button><Button variant="primary" onClick={wa}><MessageCircle size={16}/> واتساب</Button></>}/><div className="ticket-page" ref={ticket}><header><div className="ticket-logo">م</div><div><h1>شركة الماهر الماسي</h1><span>نُيسر دربك... لنطمئن قلبك</span></div><b>{b.booking_number}</b></header><div className="ticket-grid"><div><span>العميل</span><strong>{b.customer_name}</strong></div><div><span>الجوال</span><strong>{b.customer_phone}</strong></div><div><span>نوع الرحلة</span><strong>{b.journey_mode}</strong></div><div><span>السكن</span><strong>{b.accommodation_label||b.accommodation_type||'بدون'}</strong></div></div><h3>المسافرون</h3><div className="ticket-passengers">{p.map((x,i)=><div key={x.id}><b>{i+1}. {x.full_name}</b><span>{x.identity_number} • {x.nationality||''}</span></div>)}</div><div className="ticket-footer"><div><span>الإجمالي</span><b>{money(b.total_price)}</b><span>المدفوع</span><b>{money(b.paid_amount)}</b><span>المتبقي</span><b>{money(Math.max(0,Number(b.total_price||0)-Number(b.paid_amount||0)))}</b></div>{qr&&<img src={qr}/>}</div><small className="ticket-terms">يرجى الحضور في الموعد المحدد والاحتفاظ بالتذكرة والهوية. تخضع الرحلة لشروط وأحكام الشركة المسجلة بالنظام.</small></div></>}
