import React,{useEffect,useState} from 'react';import {Download,FilePlus2,RefreshCw,Printer,FileDown} from 'lucide-react';import {api} from '../../lib/api.js';import {Badge,Button,Card,ErrorBox,Field,Loading,PageHeader,Select,Table} from '../../components/UI.jsx';import {statusLabel} from '../../lib/format.js';import {basicPrintDocument,printHtmlDocument} from '../../lib/print.js';
const TYPES=[['bookings_csv','الحجوزات CSV'],['passengers_csv','المسافرون CSV'],['operations_json','التشغيل JSON'],['finance_csv','المالية CSV']];const esc=v=>String(v??'—').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
export default function Reports(){const [data,setData]=useState(null),[error,setError]=useState(''),[type,setType]=useState('bookings_csv'),[busy,setBusy]=useState(''),[orientation,setOrientation]=useState('portrait');async function load(){setError('');setData(null);try{setData(await api.module('reports'))}catch(e){setError(e.message)}}useEffect(()=>{load()},[]);async function create(e){e.preventDefault();setBusy('new');setError('');try{await api.moduleWrite({action:'insert',table:'export_jobs',row:{export_type:type,status:'pending'}});await load()}catch(e){setError(e.message)}finally{setBusy('')}}async function process(r){setBusy(r.id);setError('');try{const x=await api.moduleWrite({action:'process_export',id:r.id});if(x.signed_url)window.open(x.signed_url,'_blank','noopener,noreferrer');await load()}catch(e){setError(e.message)}finally{setBusy('')}}async function open(r){setError('');try{const x=await api.moduleWrite({action:'export_signed_url',storage_path:r.storage_path});if(x.signed_url)window.open(x.signed_url,'_blank','noopener,noreferrer')}catch(e){setError(e.message)}}const jobs=data?.export_jobs||[];function printJobs(){const rows=jobs.map(r=>`<tr><td>${esc(TYPES.find(x=>x[0]===r.export_type)?.[1]||r.export_type)}</td><td>${esc(statusLabel(r.status||'pending'))}</td><td>${esc(r.created_at?new Date(r.created_at).toLocaleString('ar-SA'):'—')}</td><td>${esc(r.completed_at?new Date(r.completed_at).toLocaleString('ar-SA'):'—')}</td></tr>`).join('');const body=`<h1>سجل التقارير والتصدير</h1><p>تم إنشاء هذا المستند من مركز التقارير، ولا يتضمن واجهة النظام.</p><table><thead><tr><th>النوع</th><th>الحالة</th><th>الطلب</th><th>الاكتمال</th></tr></thead><tbody>${rows||'<tr><td colspan="4">لا توجد طلبات تصدير</td></tr>'}</tbody></table>`;const html=basicPrintDocument({title:'سجل التقارير والتصدير',body,styles:`@page{size:A4 ${orientation==='landscape'?'landscape':'portrait'}}h1{font-size:24px}p{color:#64748b}table{width:100%;border-collapse:collapse;margin-top:16px}th,td{border:1px solid #dbe3ec;padding:8px;text-align:right}th{background:#f1f5f9}`});printHtmlDocument(html)}const cols=[{key:'export_type',label:'النوع',render:r=>TYPES.find(x=>x[0]===r.export_type)?.[1]||r.export_type},{key:'status',label:'الحالة',render:r=><Badge tone={r.status==='completed'?'green':r.status==='failed'?'red':'orange'}>{statusLabel(r.status||'pending')}</Badge>},{key:'created_at',label:'الطلب',render:r=>r.created_at?new Date(r.created_at).toLocaleString('ar-SA'):'—'},{key:'completed_at',label:'الاكتمال',render:r=>r.completed_at?new Date(r.completed_at).toLocaleString('ar-SA'):'—'},{key:'action',label:'',render:r=>r.storage_path?<Button onClick={()=>open(r)}><Download size={15}/> تنزيل</Button>:<Button onClick={()=>process(r)} disabled={busy===r.id}>{busy===r.id?'جاري التجهيز...':'تجهيز الملف'}</Button>}];return <><PageHeader title="التقارير والتصدير" subtitle="إنشاء ملفات تصدير من البيانات السحابية وحفظها مؤقتًا في Storage" actions={<Button onClick={load}><RefreshCw size={16}/> تحديث</Button>}/><ErrorBox error={error}/><Card><form className="inline-form" onSubmit={create}><Field label="تقرير جديد"><Select value={type} onChange={e=>setType(e.target.value)}>{TYPES.map(([v,l])=><option key={v} value={v}>{l}</option>)}</Select></Field><Field label="اتجاه الطباعة"><Select value={orientation} onChange={e=>setOrientation(e.target.value)}><option value="portrait">A4 عمودي</option><option value="landscape">A4 أفقي</option></Select></Field><Button variant="primary" type="submit" disabled={busy==='new'}><FilePlus2 size={16}/>{busy==='new'?'جاري الإنشاء...':'إنشاء طلب تصدير'}</Button><Button type="button" onClick={printJobs}><Printer size={16}/> طباعة السجل</Button><Button type="button" onClick={printJobs}><FileDown size={16}/> PDF</Button></form></Card>{!data&&!error?<Loading/>:<Card><div className="card-title"><h3>طلبات التصدير</h3><Badge>{jobs.length}</Badge></div><Table rows={jobs} columns={cols}/></Card>}</>}
