import React,{useEffect,useMemo,useState} from 'react';import {RefreshCw,Plus} from 'lucide-react';import {api} from '../lib/api.js';import {Card,PageHeader,Button,Loading,ErrorBox,Table,Modal,Field,Input,Textarea} from '../components/UI.jsx';
export default function GenericModule({title,subtitle,resource,tabs,defaultTable}){const [data,setData]=useState(null),[table,setTable]=useState(defaultTable||tabs?.[0]?.table),[error,setError]=useState(''),[open,setOpen]=useState(false);async function load(){setError('');setData(null);try{setData(await api.module(resource))}catch(e){setError(e.message)}}useEffect(()=>{load()},[resource]);const active=tabs?.find(x=>x.table===table)||tabs?.[0];const rows=data?.[table]||[];const columns=useMemo(()=>{const preferred=active?.columns||[];return preferred.map(k=>({key:k,label:(active?.labels?.[k]||k).replaceAll('_',' '),render:r=>typeof r[k]==='object'?JSON.stringify(r[k]):String(r[k]??'—')}))},[active]);async function save(e){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));let row={};for(const [k,v] of Object.entries(f)){if(k==='json')continue;const cfg=(active?.fields||[]).find(x=>x.name===k);row[k]=cfg?.type==='number'?(v===''?null:Number(v)):v}if(f.json){try{row={...row,...JSON.parse(f.json)}}catch{setError('JSON غير صالح');return}}try{await api.moduleWrite({action:'insert',table,row});setOpen(false);await load()}catch(e){setError(e.message)}}return <><PageHeader title={title} subtitle={subtitle} actions={<><Button onClick={load}><RefreshCw size={16}/> تحديث</Button>{active?.writable&&<Button variant="primary" onClick={()=>setOpen(true)}><Plus size={16}/> إضافة</Button>}</>}/><ErrorBox error={error}/>{tabs&&<div className="tabs">{tabs.map(t=><button key={t.table} onClick={()=>setTable(t.table)} className={table===t.table?'active':''}>{t.label}<span>{data?.[t.table]?.length??0}</span></button>)}</div>}{!data&&!error?<Loading/>:<Card>{data?._missing_tables?.includes(table)?<div className="empty">الجدول <b>{table}</b> غير موجود في قاعدة البيانات الحالية.</div>:<Table rows={rows} columns={columns}/>}</Card>}<Modal open={open} onClose={()=>setOpen(false)} title={`إضافة — ${active?.label||table}`}><form onSubmit={save} className="form-grid">{(active?.fields||[]).map(f=><Field key={f.name} label={f.label||f.name}><Input name={f.name} type={f.type||'text'} required={f.required}/></Field>)}<Field label="حقول إضافية JSON"><Textarea name="json" placeholder='{"notes":"..."}'/></Field><div className="modal-actions"><Button type="button" onClick={()=>setOpen(false)}>إلغاء</Button><Button variant="primary" type="submit">حفظ</Button></div></form></Modal></>}
