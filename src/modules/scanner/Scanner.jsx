import React,{useEffect,useRef,useState} from 'react';import {Camera,Keyboard} from 'lucide-react';import {api} from '../../lib/api.js';import {Card,PageHeader,Button,Field,Input,Select,ErrorBox} from '../../components/UI.jsx';
export default function Scanner({tripId}){const [code,setCode]=useState(''),[mode,setMode]=useState('outbound_boarding'),[result,setResult]=useState(null),[error,setError]=useState(''),[camera,setCamera]=useState(false);const video=useRef(null),stream=useRef(null);useEffect(()=>()=>stream.current?.getTracks?.().forEach(t=>t.stop()),[]);async function scan(v=code){if(!v.trim())return;setError('');try{const x=await api.moduleWrite({action:'scan',code:v.trim(),scan_mode:mode,trip_id:tripId||undefined,device_id:navigator.userAgent.slice(0,120)});setResult(x);tone(x.result==='duplicate'?440:880)}catch(e){setError(e.message);tone(220)}}function tone(freq){try{const A=new AudioContext(),o=A.createOscillator(),g=A.createGain();o.frequency.value=freq;o.connect(g);g.connect(A.destination);g.gain.value=.06;o.start();o.stop(A.currentTime+.12)}catch{}}async function startCamera(){if(!('BarcodeDetector'in window)){setError('الكاميرا المباشرة تحتاج متصفح يدعم BarcodeDetector؛ استخدم الإدخال اليدوي مؤقتًا.');return}try{stream.current=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}});video.current.srcObject=stream.current;await video.current.play();setCamera(true);const det=new BarcodeDetector({formats:['qr_code']});const loop=async()=>{if(!stream.current)return;try{const codes=await det.detect(video.current);if(codes[0]?.rawValue){setCode(codes[0].rawValue);await scan(codes[0].rawValue);stream.current.getTracks().forEach(t=>t.stop());stream.current=null;setCamera(false);return}}catch{}requestAnimationFrame(loop)};loop()}catch(e){setError(e.message)}}return <><PageHeader title="QR والصعود" subtitle="صعود، وصول، تسكين، عودة — مع منع التكرار وصوت الحالة"/><div className="scanner-grid"><Card><Field label="وضع المسح"><Select value={mode} onChange={e=>setMode(e.target.value)}><option value="outbound_boarding">صعود الذهاب</option><option value="outbound_arrival">وصول الذهاب</option><option value="housing_checkin">دخول السكن</option><option value="return_boarding">صعود العودة</option><option value="return_arrival">وصول العودة</option><option value="verify">تحقق فقط</option></Select></Field><Field label="رمز QR / رقم الحجز"><Input value={code} onChange={e=>setCode(e.target.value)} onKeyDown={e=>e.key==='Enter'&&scan()} placeholder="امسح أو اكتب الرقم"/></Field><div className="page-actions"><Button variant="primary" onClick={()=>scan()}><Keyboard size={16}/> تحقق</Button><Button onClick={startCamera}><Camera size={16}/> فتح الكاميرا</Button></div><ErrorBox error={error}/></Card><Card className="camera-card">{camera?<video ref={video} playsInline muted/>:<div className="scanner-placeholder">{result?<><strong className={result.result==='duplicate'?'warning-text':'success-text'}>{result.result==='duplicate'?'تم المسح سابقًا':'تم المسح بنجاح'}</strong><span>{result.booking?.booking_number}</span><b>{result.booking?.customer_name}</b></>:<><Camera size={50}/><span>نتيجة المسح ستظهر هنا</span></>}</div>}</Card></div></>}
